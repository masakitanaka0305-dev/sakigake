# SAKIGAKE — Lark Base 権限分離設定ガイド

## 概要
営業マンごとに「自分が紹介したエンジニアのデータのみ閲覧可能」とする権限設計。

---

## 1. Lark Base テーブル構成

### 必須フィールド

| フィールド名 | 型 | 説明 |
|-------------|------|------|
| 紹介営業マン | 単一選択 | フォームの営業マン選択肢と一致させる |
| エンジニア名 | テキスト | エンジニアの名前（イニシャル可） |
| 希望単価 | 単一選択 | 月額希望単価 |
| 稼働開始時期 | 単一選択 | 参画可能時期 |
| 経歴書 | 添付ファイル | アップロードされた経歴書 |

### 営業マン選択肢の設定
`紹介営業マン` フィールドの選択肢を以下に合わせてください：
- 山田 太郎
- 鈴木 一郎
- 佐藤 花子
- 高橋 健太
- 中村 美咲

※ フォーム側の `SALES_OPTIONS` と一致させること。

---

## 2. 行レベルの閲覧制限（ビュー分離）

### 方法A: 個人用ビューの作成（推奨）

各営業マンに専用のフィルタ付きビューを作成します。

1. Lark Base でテーブルを開く
2. 「+ ビューを追加」をクリック
3. ビュー名を「山田太郎 専用」などに設定
4. フィルタ条件を追加：
   - `紹介営業マン` = `山田 太郎`
5. ビューの共有設定で、該当営業マンのみにアクセス権を付与
6. 他の営業マンにも同様のビューを作成

### 方法B: Lark Base の高度な権限設定

Lark Base Advanced Permission（有料プラン）を使用する場合：

1. テーブル設定 → 権限管理 を開く
2. 「フィールドレベルの権限」で、各営業マンに対して：
   - `紹介営業マン` が自分と一致するレコードのみ表示
3. 編集権限は「新規追加のみ」または「閲覧のみ」に設定

---

## 3. 操作権限の設定

### 営業マン向け権限
- **閲覧**: 自分が紹介したエンジニアのレコードのみ
- **編集**: 不可（運営のみ）
- **削除**: 不可
- **エクスポート**: 不可

### 運営（管理者）権限
- **閲覧**: 全レコード
- **編集**: 全レコード
- **削除**: 可
- **エクスポート**: 可

---

## 4. Lark App 設定

### App の作成
1. [Lark Developer Console](https://open.larksuite.com/app) にアクセス
2. 「アプリを作成」→ 企業自作アプリ
3. アプリ名: `SAKIGAKE Form`

### 必要な権限スコープ
以下の権限を申請・承認してください：

| 権限 | スコープ | 用途 |
|------|---------|------|
| Bitable レコード作成 | `bitable:record:create` | フォームからのレコード追加 |
| Drive ファイルアップロード | `drive:file:upload` | 経歴書のアップロード |

### API キーの取得
1. アプリの「認証情報」タブから `App ID` と `App Secret` をコピー
2. `/Users/tanakamasaki/sakigake/config.js` に記入

### Bitable の App Token / Table ID の取得
1. Lark Base でテーブルを開く
2. URLから取得:
   ```
   https://xxx.larksuite.com/base/{APP_TOKEN}?table={TABLE_ID}
   ```
3. `config.js` の `APP_TOKEN` と `TABLE_ID` に記入

---

## 5. 通知ワークフロー（オプション）

### Lark Bot Webhook で新規登録を通知

フォーム送信後に営業マンへ自動通知する場合：

1. Lark のグループチャットで Webhook Bot を追加
2. Webhook URL を取得
3. `server.js` の送信成功後に Webhook へ POST:

```javascript
// server.js に追加（レコード作成成功後）
await fetch('YOUR_WEBHOOK_URL', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    msg_type: 'text',
    content: {
      text: `新規エンジニア登録: ${engineerName}（紹介: ${salesPerson}）`
    }
  })
});
```

---

## 6. セキュリティチェックリスト

- [ ] `config.js` を `.gitignore` に追加済み
- [ ] Lark App のスコープを最小限に設定済み
- [ ] 管理者のみがテーブルの全データを閲覧可能
- [ ] 各営業マンの個人ビューが正しくフィルタされている
- [ ] サーバーが HTTPS で運用されている（本番環境）
- [ ] ファイルアップロードサイズの上限が適切に設定されている
